import React from 'react'

const Profile = (props) => {
    return (
        <>
            <div className={`container-fulid bg-${props.mode}`}>

                <div className={`container bg-${props.mode} text-primary`}>

                    profile comming soon...
                </div>
            </div>
        </>
    )
}

export default Profile
